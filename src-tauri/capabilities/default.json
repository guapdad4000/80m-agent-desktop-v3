{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the 80m Tauri shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
